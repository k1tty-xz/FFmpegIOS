# build outputs / scratch
build/
artifacts/
*.tar.xz
*.tar.gz

# local FFmpeg source used only for introspecting ./configure options
.ffsrc/

# downloaded build artifacts / local extraction scratch
_art/
*.zip
